[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: fast isolated physics/unit checks
    integration: golden-image processor integration checks
    e2e: Playwright browser end-to-end tests
